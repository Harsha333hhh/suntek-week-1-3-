function task(){

    // initial amount value
    let amount = 0;

    // add 500 to amount and display
    console.log(amount + 500);

    // add 1200 to amount and display
    console.log(amount + 1200);

    // subtract 200 from amount and display
    console.log(amount - 200);

    // calculate 18 percent of amount
    console.log(amount * (18 / 100));

}

// call the function
task();

